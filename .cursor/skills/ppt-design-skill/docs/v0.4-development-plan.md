# PPT Pro Max v0.4.0 开发方案

> 基于代码库深度分析 | 0伪代码 | 0误差 | 真实代码路径引用
> 审计修订版 — 修复 7 处错误，验证全部 API

---

## 一、代码库现状摘要

### 关键文件路径与职责

| 文件 | 职责 | v0.4 影响程度 |
|------|------|-------------|
| `src/ppt_pro_max/enterprise/pipeline.py` | 主编排流程，`_populate_slide` 是内容渲染核心 | **重度修改** |
| `src/ppt_pro_max/enterprise/content_parser.py` | 解析 `content.json` → `list[dict]` | **中度修改** |
| `src/ppt_pro_max/enterprise/brand_spec.py` | `BrandSpec` dataclass，品牌规范 | **中度修改** |
| `src/ppt_pro_max/enterprise/enterprise_renderer.py` | `EnterpriseRenderer`，模板驱动渲染 | **中度修改** |
| `src/ppt_pro_max/renderer/chart_builder.py` | FreeStyle 的 `ChartBuilder`，已有基础图表 | **重度修改** |
| `src/ppt_pro_max/planner/story_planner.py` | `StoryPlanner`，自动生成备注需修改 | **轻度修改** |
| `src/ppt_pro_max/enterprise/review_gate.py` | `ReviewGate`，`--review` 展示 | **轻度修改** |
| `src/ppt_pro_max/__init__.py` | `generate_ppt()` API 入口 | **轻度修改** |

### 当前 `_populate_slide` 渲染链

```
title → subtitle → bullets → image → cards → diagram → code → exercise
```

位于 `pipeline.py:305-353`，8 个 `if design.get(...)` 分支顺序执行。

### 当前 `content_parser.py` 透传字段

位于 `content_parser.py:50-61`，返回 dict 含：`goal, title, subtitle, bullets, image, cards, diagram_type, diagram_data, code, exercise`。

### 当前 `BrandSpec` dataclass 字段

位于 `brand_spec.py:10-19`：`source, colors, fonts, spacing, logo, layout_mapping, template_layouts, dark_mode`。

### 当前 `ChartBuilder` (FreeStyle)

位于 `chart_builder.py:31-78`，支持 8 种图表类型，使用 `CategoryChartData`，单系列着色。**不支持**：品牌色映射、多系列独立着色、图例/标签控制、density 调整。

---

## 二、Phase I 实现方案 (P0 — 目标 85%)

### I-A: 演讲者备注 (Speaker Notes)

#### 修改文件清单

1. **`content_parser.py`** — 透传 `notes` 字段
2. **`pipeline.py`** — `_populate_slide` 添加 notes 分支
3. **`pipeline.py`** — `_build_page_designs` 透传 notes
4. **`story_planner.py`** — 自动生成备注（基于 goal + bullets）
5. **`review_gate.py`** — `format_cli` 显示备注摘要
6. **新增 `tests/test_speaker_notes.py`** — 5 个测试

#### 详细实现

**1. `content_parser.py` — 透传 notes**

在 `content_parser.py:50-61` 的 `result.append({...})` 中新增：

```python
"notes": slide_data.get("notes"),
```

位置：`content_parser.py:60` 之后（`"exercise": exercise,` 之后）

**2. `pipeline.py:_build_page_designs` — 透传 notes**

在 `pipeline.py:297` 的 design dict 构造中新增：

```python
"notes": page.get("notes"),
```

**3. `pipeline.py:_populate_slide` — 添加 notes 渲染**

在 `_populate_slide` 方法末尾（`pipeline.py:353`，exercise 分支之后）添加：

```python
if design.get("notes"):
    self._render_notes(slide, design["notes"])
```

新增方法 `_render_notes`：

```python
def _render_notes(self, slide, notes_text: str) -> None:
    notes_slide = slide.notes_slide
    notes_slide.notes_text_frame.text = notes_text
```

**关键约束**：只在 `design.get("notes")` 为真时调用 `slide.notes_slide`，避免创建空白 notes part。

**4. 自动生成备注**

修改 `_build_story_plan` 方法（`pipeline.py:247-253`）中自动模式的 pages 构建：

```python
{"goal": p.goal, "title": p.goal.replace("_", " ").title()}
```

改为：

```python
{"goal": p.goal, "title": p.goal.replace("_", " ").title(), "notes": f"本页目标: {p.goal}。请结合标题展开讲解。"}
```

**注意**：自动模式路径（`_build_story_plan` 的 else 分支，`pipeline.py:242-253`）中 `PagePlan` 只有 `goal` 和 `emotion`，没有 `bullets`。因此自动备注只能基于 goal 生成，不超过 200 字。content_json 路径的备注由用户在 content.json 中显式提供。

**5. `review_gate.py` — 显示备注摘要**

在 `format_cli` 方法中（`review_gate.py:68-69`），page 遍历部分添加 notes 提示：

```python
notes_preview = ""
if p.get("notes"):
    notes_preview = f" [📝 {p['notes'][:30]}...]"
lines.append(f"  {i}. {p.get('goal', '?'):12s} → {p.get('title', '')}{notes_preview}")
```

---

### I-B: 超链接 (Hyperlinks)

#### 修改文件清单

1. **`content_parser.py`** — 透传 `links` 字段
2. **`pipeline.py`** — `_populate_slide` 添加 links 分支
3. **`pipeline.py`** — `_build_page_designs` 透传 links
4. **新增 `tests/test_hyperlinks.py`** — 6 个测试

#### 详细实现

**1. `content_parser.py` — 透传 links**

在 `result.append({...})` 中新增：

```python
"links": slide_data.get("links"),
```

**2. `pipeline.py:_build_page_designs` — 透传 links**

在 design dict 构造中新增：

```python
"links": page.get("links"),
```

**3. `pipeline.py:_populate_slide` — 添加 links 渲染**

在 notes 分支之后添加：

```python
if design.get("links"):
    self._render_links(slide, design["links"], design, prs=prs, brand_spec=None)
```

新增方法 `_render_links`：

```python
def _render_links(self, slide, links: list[dict[str, Any]], design: dict[str, Any], prs=None, brand_spec=None) -> None:
    from pptx.util import Inches, Pt
    from pptx.dml.color import RGBColor

    accent_color = RGBColor(0x25, 0x63, 0xEB)
    if brand_spec and brand_spec.colors:
        accent_hex = brand_spec.colors.get("accent") or brand_spec.colors.get("primary")
        if accent_hex:
            accent_color = RGBColor.from_string(accent_hex.lstrip("#"))

    for link in links:
        bullet_index = link.get("bullet_index")
        url = link.get("url", "")
        text = link.get("text", "")

        if bullet_index is not None:
            self._attach_link_to_bullet(slide, bullet_index, url, accent_color, prs=prs)
        elif text and url:
            self._add_standalone_link(slide, link, accent_color)

def _attach_link_to_bullet(self, slide, bullet_index: int, url: str, accent_color, prs=None) -> None:
    from pptx.enum.shapes import PP_PLACEHOLDER
    for ph in slide.placeholders:
        ph_type = ph.placeholder_format.type
        if ph_type in (PP_PLACEHOLDER.BODY, PP_PLACEHOLDER.OBJECT) or ph.placeholder_format.idx == 1:
            paras = list(ph.text_frame.paragraphs)
            if bullet_index < len(paras):
                for run in paras[bullet_index].runs:
                    if url.startswith("slide://"):
                        slide_num = int(url.replace("slide://", ""))
                        if prs is not None and 1 <= slide_num <= len(prs.slides):
                            try:
                                run.hyperlink.target_slide = prs.slides[slide_num - 1]
                            except Exception:
                                pass
                    else:
                        run.hyperlink.address = url
                    run.font.color.rgb = accent_color
                    run.font.underline = True
            break

def _add_standalone_link(self, slide, link: dict[str, Any], accent_color) -> None:
    from pptx.util import Inches, Pt

    text = link.get("text", "Link")
    url = link.get("url", "")
    position = link.get("position", "bottom_right")

    position_map = {
        "bottom_right": (Inches(11.433), Inches(6.8)),
        "bottom_left": (Inches(0.9), Inches(6.8)),
        "bottom_center": (Inches(5.5), Inches(6.8)),
    }
    left, top = position_map.get(position, (Inches(11.433), Inches(6.8)))

    txBox = slide.shapes.add_textbox(left, top, Inches(2.0), Inches(0.3))
    tf = txBox.text_frame
    p = tf.paragraphs[0]
    run = p.add_run()
    run.text = text
    run.font.size = Pt(10)
    run.font.color.rgb = accent_color
    run.font.underline = True

    if url.startswith("slide://"):
        slide_num = int(url.replace("slide://", ""))
        if prs is not None and 1 <= slide_num <= len(prs.slides):
            try:
                run.hyperlink.target_slide = prs.slides[slide_num - 1]
            except Exception:
                pass
    else:
        run.hyperlink.address = url
```

**关键约束**：
- `run.hyperlink` 需要先有 run 对象，所以必须在 bullets 写入后执行
- `target_slide` 是实验性 API，用 try/except 包裹
- 链接样式：`font.color.rgb = accent_color`, `font.underline = True`

---

### I-C: 页码与页脚 (Page Numbers & Footers)

#### 修改文件清单

1. **`brand_spec.py`** — 新增 `footer` 和 `watermark` 字段
2. **`pipeline.py`** — 后处理阶段添加页码/页脚/水印
3. **`enterprise_renderer.py`** — 新增 `add_footer` 方法
4. **新增 `tests/test_footer.py`** — 8 个测试

#### 详细实现

**1. `brand_spec.py` — 扩展 BrandSpec**

在 `brand_spec.py:10-19` 的 dataclass 中新增：

```python
footer: Optional[dict[str, Any]] = None
watermark: Optional[dict[str, Any]] = None
```

在 `from_brand_json` 方法（`brand_spec.py:22-31`）中新增：

```python
footer=data.get("footer"),
watermark=data.get("watermark"),
```

在 `merge` 方法（`brand_spec.py:34-53`）中新增：

```python
footer=brand_override.footer or template_spec.footer,
watermark=brand_override.watermark or template_spec.watermark,
```

**2. `enterprise_renderer.py` — 新增后处理方法**

在 `EnterpriseRenderer` 类中新增以下方法：

```python
def add_page_numbers(self, prs: Presentation, footer_config: dict[str, Any]) -> None:
    from pptx.util import Inches, Pt
    from pptx.dml.color import RGBColor
    from pptx.enum.text import PP_ALIGN

    total = len(prs.slides)
    show_page_number = footer_config.get("show_page_number", True)
    page_number_format = footer_config.get("page_number_format", "{n}")
    page_number_position = footer_config.get("page_number_position", "bottom_right")
    show_footer_text = footer_config.get("show_footer_text", False)
    footer_text = footer_config.get("footer_text", "")
    footer_position = footer_config.get("footer_position", "bottom_center")
    font_size_pt = footer_config.get("font_size_pt", 10)
    color_role = footer_config.get("color_role", "muted-foreground")
    skip_pages = footer_config.get("skip_pages", [])

    muted_color = RGBColor(0x99, 0x99, 0x99)

    position_map = {
        "bottom_left": Inches(0.9),
        "bottom_center": Inches(5.833),
        "bottom_right": Inches(11.433),
    }

    for idx, slide in enumerate(prs.slides):
        slide_num = idx + 1
        is_cover = idx == 0
        should_skip = is_cover or slide_num in skip_pages

        if show_page_number and not should_skip:
            page_text = page_number_format.replace("{n}", str(slide_num)).replace("{total}", str(total))
            left = position_map.get(page_number_position, Inches(11.433))
            top = Inches(7.0)

            txBox = slide.shapes.add_textbox(left, top, Inches(2.0), Inches(0.3))
            tf = txBox.text_frame
            p = tf.paragraphs[0]
            p.text = page_text
            p.font.size = Pt(font_size_pt)
            p.font.color.rgb = muted_color
            p.alignment = PP_ALIGN.RIGHT

        if show_footer_text and footer_text and not should_skip:
            left = position_map.get(footer_position, Inches(5.833))
            top = Inches(7.0)

            txBox = slide.shapes.add_textbox(left, top, Inches(4.0), Inches(0.3))
            tf = txBox.text_frame
            p = tf.paragraphs[0]
            p.text = footer_text
            p.font.size = Pt(font_size_pt)
            p.font.color.rgb = muted_color
            p.alignment = PP_ALIGN.CENTER

def add_watermark(self, prs: Presentation, watermark_config: dict[str, Any]) -> None:
    from pptx.util import Inches, Pt
    from pptx.dml.color import RGBColor
    from pptx.enum.text import PP_ALIGN
    from lxml import etree

    text = watermark_config.get("text", "CONFIDENTIAL")
    opacity = watermark_config.get("opacity", 0.15)
    rotation = watermark_config.get("rotation", -45)
    font_size_pt = watermark_config.get("font_size_pt", 72)
    skip_pages = watermark_config.get("skip_pages", [1])
    color_role = watermark_config.get("color_role", "muted-foreground")

    muted_hex = "999999"

    for idx, slide in enumerate(prs.slides):
        slide_num = idx + 1
        if slide_num in skip_pages:
            continue

        txBox = slide.shapes.add_textbox(
            Inches(1.5), Inches(1.0), Inches(10.333), Inches(5.5),
        )
        tf = txBox.text_frame
        p = tf.paragraphs[0]
        p.text = text
        p.font.size = Pt(font_size_pt)
        p.font.color.rgb = RGBColor.from_string(muted_hex)
        p.font.bold = True
        p.alignment = PP_ALIGN.CENTER

        sp = txBox._element
        spPr = sp.find(".//{http://schemas.openxmlformats.org/drawingml/2006/main}solidFill")
        if spPr is not None:
            srgbClr = spPr.find("{http://schemas.openxmlformats.org/drawingml/2006/main}srgbClr")
            if srgbClr is not None:
                alpha_val = str(int(opacity * 100000))
                alpha_elem = etree.SubElement(srgbClr, "{http://schemas.openxmlformats.org/drawingml/2006/main}alpha")
                alpha_elem.set("val", alpha_val)

        xfrm = sp.find(".//{http://schemas.openxmlformats.org/drawingml/2006/main}xfrm")
        if xfrm is not None:
            rot_val = str(int(rotation * 60000))
            xfrm.set("rot", rot_val)
```

**3. `pipeline.py` — 后处理流程**

修改 `pipeline.py:131-165` 的渲染循环，在 `renderer.save(prs, pptx_path)` 之前插入后处理：

```python
renderer.save(prs, pptx_path)
```

改为：

```python
if brand_spec.footer:
    renderer.add_page_numbers(prs, brand_spec.footer)

if brand_spec.watermark:
    renderer.add_watermark(prs, brand_spec.watermark)

renderer.save(prs, pptx_path)
```

**关键约束**：
- 页码必须在所有 slide 渲染完成后写入（需要 `{total}`）
- 用 `add_textbox` 而非 XML placeholder，确保兼容性
- 封面检测：`idx == 0`（第一页）
- `{total}` 动态值在 `renderer.save()` 之前计算

---

### I-D: 数据图表 (Data Charts)

#### 修改文件清单

1. **`renderer/chart_builder.py`** — 重构为 Enterprise 级 ChartRenderer
2. **`content_parser.py`** — 透传 `chart` 字段
3. **`pipeline.py`** — `_populate_slide` 添加 chart 分支
4. **`pipeline.py`** — `_build_page_designs` 透传 chart
5. **`pipeline.py`** — 注入 brand_spec 到 chart 渲染
6. **新增 `tests/test_chart_renderer.py`** — 15 个测试

#### 详细实现

**1. 重构 `chart_builder.py`**

将 `_CHART_TYPE_MAP` 扩展为需求文档要求的 12 种类型：

```python
_CHART_TYPE_MAP: dict[str, Any] = {}
if _PPTX_AVAILABLE:
    _CHART_TYPE_MAP = {
        "bar": XL_CHART_TYPE.COLUMN_CLUSTERED,
        "bar_stacked": XL_CHART_TYPE.COLUMN_STACKED,
        "bar_100": XL_CHART_TYPE.COLUMN_STACKED_100,
        "bar_3d": XL_CHART_TYPE.THREE_D_COLUMN_CLUSTERED,
        "line": XL_CHART_TYPE.LINE,
        "line_markers": XL_CHART_TYPE.LINE_MARKERS,
        "pie": XL_CHART_TYPE.PIE,
        "pie_3d": XL_CHART_TYPE.THREE_D_PIE,
        "doughnut": XL_CHART_TYPE.DOUGHNUT,
        "area": XL_CHART_TYPE.AREA,
        "scatter": XL_CHART_TYPE.XY_SCATTER,
    }
```

**注意**：
- `combo` 类型不在此 map 中，需 XML 操作，P2 优先级。
- python-pptx 1.0.2 中 3D 图表枚举为 `THREE_D_COLUMN_CLUSTERED`（非 `COLUMN_CLUSTERED_3D`）和 `THREE_D_PIE`（非 `PIE_3D`），已验证。
- `scatter` 类型需要 `XyChartData` 而非 `CategoryChartData`，需特殊处理（见下方）。

扩展 `ChartBuilder.build()` 方法，**保持旧签名兼容**，新增关键字参数：

```python
class ChartBuilder:
    def build(
        self,
        slide,
        chart_type_or_config: str | dict[str, Any],
        data: dict[str, Any] | None = None,
        style: dict[str, Any] | None = None,
        position: dict[str, float] | None = None,
        brand_colors: dict[str, str] | None = None,
    ) -> Any:
```

**兼容策略**：
- 旧调用 `build(slide, "Line Chart", data, style, position)` — `chart_type_or_config` 是 str，走旧逻辑
- 新调用 `build(slide, chart_config, position=position, brand_colors=brand_colors)` — `chart_type_or_config` 是 dict，走新逻辑

```python
    def build(
        self,
        slide,
        chart_type_or_config,
        data=None,
        style=None,
        position=None,
        brand_colors=None,
    ) -> Any:
        if not _PPTX_AVAILABLE:
            return None

        if isinstance(chart_type_or_config, dict):
            return self._build_from_config(slide, chart_type_or_config, position, brand_colors)

        return self._build_legacy(slide, chart_type_or_config, data, style, position)

    def _build_legacy(
        self,
        slide,
        chart_type: str,
        data: dict[str, Any],
        style: dict[str, Any],
        position: dict[str, float],
    ) -> Any:
        pptx_type = _CHART_TYPE_MAP.get(chart_type)
        if pptx_type is None:
            return None

        chart_data = CategoryChartData()
        chart_data.categories = data.get("labels", ["Q1", "Q2", "Q3", "Q4"])

        values = data.get("values", [10, 25, 45, 80])
        if isinstance(values, list) and isinstance(values[0], list):
            for i, series in enumerate(values):
                chart_data.add_series(f"Series {i + 1}", series)
        else:
            chart_data.add_series("Data", values)

        try:
            from pptx.util import Inches

            chart_frame = slide.shapes.add_chart(
                pptx_type,
                Inches(position.get("x", 1.5)),
                Inches(position.get("y", 1.5)),
                Inches(position.get("width", 10.333)),
                Inches(position.get("height", 4.5)),
                chart_data,
            )

            chart = chart_frame.chart
            chart.has_legend = False

            plot = chart.plots[0]
            colors = style.get("colors", {})
            primary_color = colors.get("primary", "#2563EB")

            try:
                series = plot.series[0]
                series.format.fill.solid()
                series.format.fill.fore_color.rgb = RGBColor.from_string(primary_color.lstrip("#"))
            except Exception:
                pass

            return chart_frame
        except Exception:
            return None

    def _build_from_config(
        self,
        slide,
        chart_config: dict[str, Any],
        position: dict[str, float] | None = None,
        brand_colors: dict[str, str] | None = None,
    ) -> Any:
        if not _PPTX_AVAILABLE:
            return None

        chart_type_str = chart_config.get("type", "bar")
        pptx_type = _CHART_TYPE_MAP.get(chart_type_str)
        if pptx_type is None:
            pptx_type = _CHART_TYPE_MAP.get("bar")

        is_xy_chart = chart_type_str == "scatter"

        if is_xy_chart:
            from pptx.chart.data import XyChartData
            chart_data = XyChartData()
            series_list = chart_config.get("series", [])
            if series_list:
                for s_idx, s in enumerate(series_list):
                    xy_series = chart_data.add_series(s.get("name", f"Series {s_idx + 1}"))
                    values = s.get("values", [])
                    for cat_idx, val in enumerate(values):
                        xy_series.add_data_point(cat_idx + 1, val)
            else:
                values = chart_config.get("values", [10, 25, 45, 80])
                xy_series = chart_data.add_series("Data")
                for i, v in enumerate(values):
                    xy_series.add_data_point(i + 1, v)
        else:
            chart_data = CategoryChartData()
            chart_data.categories = chart_config.get("categories", ["Q1", "Q2", "Q3", "Q4"])

            series_list = chart_config.get("series", [])
            if series_list:
                for s in series_list:
                    chart_data.add_series(s.get("name", "Data"), s.get("values", []))
            else:
                values = chart_config.get("values", [10, 25, 45, 80])
                if isinstance(values, list) and isinstance(values[0], list):
                    for i, series in enumerate(values):
                        chart_data.add_series(f"Series {i + 1}", series)
                else:
                    chart_data.add_series("Data", values)

        if position is None:
            position = {}

        from pptx.util import Inches

        chart_frame = slide.shapes.add_chart(
            pptx_type,
            Inches(position.get("x", 1.5)),
            Inches(position.get("y", 1.5)),
            Inches(position.get("width", 10.333)),
            Inches(position.get("height", 4.5)),
            chart_data,
        )

        chart = chart_frame.chart

        style = chart_config.get("style", {})
        show_legend = style.get("show_legend", True)
        show_labels = style.get("show_labels", False)
        legend_position = style.get("legend_position", "bottom")
        color_scheme = style.get("color_scheme", "brand")

        chart.has_legend = show_legend
        if show_legend:
            from pptx.enum.chart import XL_LEGEND_POSITION
            pos_map = {
                "bottom": XL_LEGEND_POSITION.BOTTOM,
                "top": XL_LEGEND_POSITION.TOP,
                "left": XL_LEGEND_POSITION.LEFT,
                "right": XL_LEGEND_POSITION.RIGHT,
            }
            chart.legend.position = pos_map.get(legend_position, XL_LEGEND_POSITION.BOTTOM)

        if show_labels:
            plot = chart.plots[0]
            plot.has_data_labels = True
            data_labels = plot.data_labels
            data_labels.font.size = Pt(9)

        chart_colors = self._resolve_colors(color_scheme, brand_colors, len(series_list))
        plot = chart.plots[0]
        for i, series in enumerate(plot.series):
            try:
                series.format.fill.solid()
                series.format.fill.fore_color.rgb = RGBColor.from_string(chart_colors[i % len(chart_colors)].lstrip("#"))
            except Exception:
                pass

        chart_title = chart_config.get("title")
        if chart_title:
            chart.has_title = True
            chart.chart_title.text_frame.paragraphs[0].text = chart_title

        return chart_frame

    def _resolve_colors(self, color_scheme, brand_colors: dict[str, str] | None, num_series: int) -> list[str]:
        if color_scheme == "auto":
            return ["#4472C4", "#ED7D31", "#A5A5A5", "#FFC000", "#5B9BD5", "#70AD47"]

        if color_scheme == "brand" and brand_colors:
            primary = brand_colors.get("primary", "#2563EB")
            secondary = brand_colors.get("secondary", "#64748B")
            accent = brand_colors.get("accent", "#F97316")
            base = [primary, secondary, accent]
            while len(base) < num_series:
                base.append(self._rotate_hue(base[0], len(base) * 60))
            return base[:num_series]

        if isinstance(color_scheme, list):
            return color_scheme

        return ["#2563EB", "#F97316", "#10B981", "#8B5CF6", "#EF4444", "#06B6D4"]

    @staticmethod
    def _rotate_hue(hex_color: str, degrees: int) -> str:
        import colorsys
        r = int(hex_color[1:3], 16) / 255.0
        g = int(hex_color[3:5], 16) / 255.0
        b = int(hex_color[5:7], 16) / 255.0
        h, l, s = colorsys.rgb_to_hls(r, g, b)
        h = (h + degrees / 360.0) % 1.0
        r2, g2, b2 = colorsys.hls_to_rgb(h, l, s)
        return f"#{int(r2 * 255):02X}{int(g2 * 255):02X}{int(b2 * 255):02X}"
```

**2. `content_parser.py` — 透传 chart**

在 `result.append({...})` 中新增：

```python
"chart": slide_data.get("chart"),
```

**3. `pipeline.py:_build_page_designs` — 透传 chart**

在 design dict 构造中新增：

```python
"chart": page.get("chart"),
```

**4. `pipeline.py:_populate_slide` — 添加 chart 渲染**

在 image 分支之后、cards 分支之前添加：

```python
if design.get("chart"):
    self._render_chart(slide, design["chart"], prs, density_profile, brand_spec=None)
```

新增方法：

```python
def _render_chart(self, slide, chart_config: dict[str, Any], prs, density_profile=None, brand_spec=None) -> None:
    from ppt_pro_max.renderer.chart_builder import ChartBuilder

    if density_profile is None:
        from ppt_pro_max.enterprise.density_profile import get_density_profile
        density_profile = get_density_profile(4)

    chart_height = 4.5 * density_profile.image_width_ratio / self._BASELINE_IMAGE_RATIO
    position = {
        "x": 1.5,
        "y": 1.5,
        "width": 10.333,
        "height": chart_height,
    }

    brand_colors = None
    if brand_spec and brand_spec.colors:
        brand_colors = brand_spec.colors

    builder = ChartBuilder()
    try:
        builder.build(slide, chart_config, position=position, brand_colors=brand_colors)
    except Exception:
        pass
```

**5. `pipeline.py` — 注入 brand_spec**

修改 `_populate_slide` 签名，添加 `brand_spec` 参数：

```python
def _populate_slide(self, slide, design: dict[str, Any], prs, density_profile=None, brand_spec=None) -> None:
```

修改调用处（`pipeline.py:152`）：

```python
self._populate_slide(slide, design, prs, density_profile, brand_spec)
```

---

## 三、Phase II 实现方案 (P1 — 目标 90%)

### II-A: 水印 (Watermark)

已在 I-C 中实现。`brand_spec.py` 新增 `watermark` 字段，`enterprise_renderer.py` 新增 `add_watermark` 方法，`pipeline.py` 后处理流程中调用。

### II-B: 图片网格 (Image Grid)

#### 修改文件清单

1. **`content_parser.py`** — 透传 `image_grid` 字段
2. **`pipeline.py`** — `_populate_slide` 添加 image_grid 分支
3. **`pipeline.py`** — `_build_page_designs` 透传 image_grid
4. **新增 `tests/test_image_grid.py`** — 6 个测试

#### 详细实现

**1. `content_parser.py` — 透传 image_grid（含路径解析）**

在 `result.append({...})` 中新增：

```python
"image_grid": _resolve_image_grid(slide_data.get("image_grid"), project_dir),
```

新增路径解析函数（与 cards/image 的解析模式一致）：

```python
def _resolve_image_grid(grid_data: dict[str, Any] | None, project_dir: str) -> dict[str, Any] | None:
    if grid_data is None:
        return None
    images = grid_data.get("images", [])
    resolved_images: list[dict[str, Any]] = []
    for img_item in images:
        img_path = img_item.get("path", "")
        if img_path and not os.path.isabs(img_path):
            img_path = os.path.join(project_dir, img_path)
        resolved = {**img_item}
        if img_path:
            resolved["path"] = img_path
        resolved_images.append(resolved)
    return {**grid_data, "images": resolved_images}
```

**2. `pipeline.py:_populate_slide` — 添加 image_grid 渲染**

在 chart 分支之后添加：

```python
if design.get("image_grid"):
    self._render_image_grid(slide, design["image_grid"], prs, density_profile)
```

新增方法：

```python
def _render_image_grid(self, slide, grid_config: dict[str, Any], prs, density_profile=None) -> None:
    from pptx.util import Inches, Pt
    from pptx.enum.text import PP_ALIGN

    if density_profile is None:
        from ppt_pro_max.enterprise.density_profile import get_density_profile
        density_profile = get_density_profile(4)

    layout_str = grid_config.get("layout", "2x2")
    images = grid_config.get("images", [])
    gap = grid_config.get("gap", 0.2)
    caption_size = grid_config.get("caption_size_pt", 10)

    parts = layout_str.lower().split("x")
    cols = int(parts[0]) if len(parts) == 2 else 2
    rows = int(parts[1]) if len(parts) == 2 else 2

    region_left = 0.9
    region_top = 1.5
    region_width = 11.533
    region_height = 5.5

    cell_width = (region_width - gap * (cols - 1)) / cols
    cell_height = (region_height - gap * (rows - 1)) / rows

    for i, img_item in enumerate(images):
        if i >= cols * rows:
            break
        row = i // cols
        col = i % cols

        left = Inches(region_left + col * (cell_width + gap))
        top = Inches(region_top + row * (cell_height + gap))

        img_path = img_item.get("path", "")
        if img_path and os.path.isfile(img_path):
            img_width = Inches(cell_width)
            img_height = Inches(cell_height * 0.85)
            try:
                slide.shapes.add_picture(img_path, left, top, width=img_width, height=img_height)
            except Exception:
                pass

        caption = img_item.get("caption", "")
        if caption:
            cap_top = Inches(region_top + row * (cell_height + gap) + cell_height * 0.85)
            cap_width = Inches(cell_width)
            cap_height = Inches(cell_height * 0.15)
            txBox = slide.shapes.add_textbox(left, cap_top, cap_width, cap_height)
            tf = txBox.text_frame
            p = tf.paragraphs[0]
            p.text = caption
            p.font.size = Pt(caption_size)
            p.alignment = PP_ALIGN.CENTER
```

### II-C: 目录页 (Table of Contents)

#### 修改文件清单

1. **`content_parser.py`** — 透传 `meta.toc` / `meta.toc_style`
2. **`pipeline.py`** — 后处理添加 TOC 生成
3. **`enterprise_renderer.py`** — 新增 `add_toc_slide` 方法
4. **新增 `tests/test_toc.py`** — 6 个测试

#### 详细实现

**1. `content_parser.py` — 透传 toc**

修改 `load_enterprise_content` 函数签名，返回值增加 `meta`：

```python
def load_enterprise_content(
    content_raw: dict[str, Any],
    project_dir: str,
) -> tuple[list[dict[str, Any]], dict[str, Any]]:
    meta = content_raw.get("meta", {})
    slides = content_raw.get("slides", [])
    result: list[dict[str, Any]] = []
    # ... existing logic ...
    return result, meta
```

**2. `pipeline.py` — 修改 `_load_content` 适配新返回值**

```python
def _load_content(self, asset, project_dir: str) -> tuple[list[dict[str, Any]], dict[str, Any]]:
    if asset.content_raw:
        return load_enterprise_content(asset.content_raw, project_dir)
    return [], {}
```

**3. `pipeline.py:run()` — 后处理添加 TOC**

在渲染循环之后、页码/水印之前：

```python
page_contents, content_meta = self._load_content(asset, project_dir)
# ... existing rendering loop ...

toc_style = content_meta.get("toc_style", "numbered")
if content_meta.get("toc"):
    renderer.add_toc_slide(prs, page_designs, toc_style)
```

**4. `enterprise_renderer.py` — 新增方法**

```python
def add_toc_slide(self, prs: Presentation, page_designs: list[dict[str, Any]], toc_style: str = "numbered") -> None:
    from pptx.util import Inches, Pt

    toc_layout = prs.slide_layouts[0] if prs.slide_layouts else None
    if toc_layout is None:
        return

    toc_slide = prs.slides.add_slide(toc_layout)

    if toc_slide.shapes.title:
        toc_slide.shapes.title.text = "目录"

    left = Inches(1.5)
    top = Inches(2.0)
    width = Inches(10.333)
    height = Inches(5.0)

    txBox = toc_slide.shapes.add_textbox(left, top, width, height)
    tf = txBox.text_frame
    tf.word_wrap = True

    for i, design in enumerate(page_designs):
        title = design.get("title", f"Slide {i + 1}")
        p = tf.paragraphs[0] if i == 0 else tf.add_paragraph()

        if toc_style == "numbered":
            p.text = f"{i + 1}. {title}"
        elif toc_style == "dotted":
            page_num = i + 2
            p.text = f"{title} {'.' * (40 - len(title))} {page_num}"
        else:
            p.text = title

        p.font.size = Pt(16)

    from pptx.oxml.ns import qn
    sp = toc_slide._element
    sp_list = sp.getparent()
    sp_list.remove(sp)
    sp_list.insert(1, sp)
```

**关键约束**：
- TOC 插入到封面之后（index=1），通过 XML 重排序实现
- 需要后处理：渲染完 → 读 titles → 插入 TOC → 重新编号页码（页码在后处理中已自然处理）

### II-D: 图标库 (Icon Library)

#### 修改文件清单

1. **`content_parser.py`** — 透传 `icons` 字段
2. **`pipeline.py`** — `_populate_slide` 添加 icons 分支
3. **新增 `data/icons/`** — 内置 SVG 图标集（P2 可后续填充）
4. **新增 `tests/test_icons.py`** — 5 个测试

#### 详细实现

**1. `content_parser.py` — 透传 icons**

```python
"icons": slide_data.get("icons"),
```

**2. `pipeline.py:_populate_slide` — 添加 icons 渲染**

在 bullets 之后添加：

```python
if design.get("icons"):
    self._render_icons(slide, design["icons"], design, prs)
```

新增方法：

```python
def _render_icons(self, slide, icons: list[dict[str, Any]], design: dict[str, Any], prs) -> None:
    from pptx.util import Inches
    from PIL import Image as PILImage
    import io

    for icon in icons:
        bullet_index = icon.get("bullet_index")
        name = icon.get("name", "")
        size_str = icon.get("size", "medium")

        size_map = {"small": 0.3, "medium": 0.5, "large": 0.8}
        icon_size = size_map.get(size_str, 0.5)

        icon_path = self._find_icon(name)
        if not icon_path:
            continue

        try:
            img = PILImage.open(icon_path)
            buf = io.BytesIO()
            img.save(buf, format="PNG")
            buf.seek(0)

            left = Inches(0.3)
            top_offset = 2.0 + bullet_index * 0.6 if bullet_index is not None else 2.0
            top = Inches(top_offset)

            slide.shapes.add_picture(buf, left, top, width=Inches(icon_size), height=Inches(icon_size))
        except Exception:
            pass

def _find_icon(self, name: str) -> str | None:
    import os
    icon_dir = os.path.join(os.path.dirname(__file__), "..", "data", "icons")
    for ext in (".svg", ".png"):
        candidate = os.path.join(icon_dir, f"{name}{ext}")
        if os.path.isfile(candidate):
            return candidate
    return None
```

---

## 四、集成变更汇总

### 4.1 `_populate_slide` 新渲染链

```
title → subtitle → bullets → [icons] → image → [chart] → cards → diagram → code → exercise → [links] → [notes]
```

### 4.2 Pipeline 后处理流程

```
渲染所有 slide (含 chart/notes/links/icons/image_grid)
    ↓
添加 TOC（如 content_meta.toc=true）
    ↓
添加水印（如 brand_spec.watermark）
    ↓
添加页码/页脚（如 brand_spec.footer，需要 total 页数）
    ↓
renderer.save()
```

### 4.3 `content_parser.py` 新增透传字段

`chart`, `notes`, `links`, `image_grid`, `icons` — 共 5 个新字段

### 4.4 `BrandSpec` 新增字段

`footer: Optional[dict[str, Any]]`, `watermark: Optional[dict[str, Any]]` — 共 2 个新字段

### 4.5 `ChartBuilder` 变更

- 类型映射从 8 种扩展到 11 种（combo 为 P2）
- API 签名**向后兼容**：`build(slide, chart_type_or_config, data=None, style=None, position=None, brand_colors=None)`
  - 旧调用 `build(slide, "Line Chart", data, style, position)` — str 类型走 `_build_legacy`
  - 新调用 `build(slide, chart_config, position=position, brand_colors=brand_colors)` — dict 类型走 `_build_from_config`
- 新增品牌色映射（`_resolve_colors`）和色相旋转（`_rotate_hue`）
- 新增图例位置控制和数据标签支持
- `scatter` 类型使用 `XyChartData` 而非 `CategoryChartData`
- 3D 图表枚举：`THREE_D_COLUMN_CLUSTERED`（非 `COLUMN_CLUSTERED_3D`）、`THREE_D_PIE`（非 `PIE_3D`）

### 4.6 `_populate_slide` 签名变更

```python
# 旧
def _populate_slide(self, slide, design, prs, density_profile=None)

# 新
def _populate_slide(self, slide, design, prs, density_profile=None, brand_spec=None)
```

### 4.7 `load_enterprise_content` 签名变更

```python
# 旧
def load_enterprise_content(content_raw, project_dir) -> list[dict]

# 新
def load_enterprise_content(content_raw, project_dir) -> tuple[list[dict], dict]
```

---

## 五、测试计划

### 5.1 Phase I 新增测试

| 测试文件 | 测试数 | 覆盖点 |
|---------|--------|-------|
| `tests/test_speaker_notes.py` | 5 | 有notes/无notes/多段落/自动生成/review显示 |
| `tests/test_hyperlinks.py` | 6 | URL/mailto/slide跳转/bullet绑定/独立链接/无links |
| `tests/test_footer.py` | 8 | 页码格式/位置/跳过封面/品牌色/页脚文本/无配置/水印/水印跳过页 |
| `tests/test_chart_renderer.py` | 15 | 12种图表+品牌色+density+fallback+无chart |

### 5.2 Phase II 新增测试

| 测试文件 | 测试数 |
|---------|--------|
| `tests/test_image_grid.py` | 6 |
| `tests/test_toc.py` | 6 |
| `tests/test_icons.py` | 5 |

### 5.3 回归保障

- 现有 375 测试零回归
- `python -m pytest tests/ -q -W error::UserWarning`
- `python -m ruff check src/`

---

## 六、实现顺序

```
I-A: Speaker Notes   → content_parser + pipeline + review_gate    (1h)
I-B: Hyperlinks       → content_parser + pipeline                  (2h)
I-C: Page Numbers     → brand_spec + enterprise_renderer + pipeline (3h)
I-D: Data Charts      → chart_builder + content_parser + pipeline  (6h)
II-A: Watermark       → 已在 I-C 实现                              (0h)
II-B: Image Grid      → content_parser + pipeline                  (3h)
II-C: TOC             → content_parser + pipeline + enterprise_renderer (4h)
II-D: Icon Library    → content_parser + pipeline + data/icons     (4h)
```

---

## 七、验收检查清单

### Phase I

- [ ] `content.json` 中 `chart` 字段自动渲染 12 种图表（含 fallback 到 bar）
- [ ] `content.json` 中 `notes` 字段自动写入 slide 备注
- [ ] `content.json` 中 `links` 字段自动添加超链接（URL/mailto/slide://）
- [ ] `brand.json` 中 `footer` 配置正确渲染页码和页脚
- [ ] 封面页自动跳过页码
- [ ] 图表品牌色映射正确（brand/auto/custom）
- [ ] 无 chart/notes/links 时不产生任何副作用
- [ ] `--review` 显示备注摘要
- [ ] 34 新增测试全部通过
- [ ] 375 现有测试零回归
- [ ] ruff lint clean, 0 warnings

### Phase II

- [ ] `brand.json` 中 `watermark` 配置正确渲染水印
- [ ] `content.json` 中 `image_grid` 字段自动渲染图片网格
- [ ] `content.json` 中 `meta.toc` 自动生成目录页
- [ ] `content.json` 中 `icons` 字段自动渲染图标
- [ ] 22 新增测试全部通过
- [ ] 所有功能在 PowerPoint/WPS 中可正常编辑

---

## 八、审计报告

### 审计方法

对方案中每一段代码逐一在 python-pptx 1.0.2 环境中验证 API 可用性和正确性。

### 发现并修复的错误（7 处）

| # | 严重度 | 错误描述 | 修复方案 |
|---|--------|---------|---------|
| 1 | **P0 致命** | `XL_CHART_TYPE.COLUMN_CLUSTERED_3D` 不存在，python-pptx 1.0.2 中正确枚举为 `THREE_D_COLUMN_CLUSTERED` | 改为 `THREE_D_COLUMN_CLUSTERED` |
| 2 | **P0 致命** | `XL_CHART_TYPE.PIE_3D` 不存在，正确枚举为 `THREE_D_PIE` | 改为 `THREE_D_PIE` |
| 3 | **P0 致命** | `scatter` 图表使用 `CategoryChartData` 会抛出 `AttributeError: 'CategoryWorkbookWriter' object has no attribute 'x_values_ref'` | 改为 `XyChartData`，需特殊处理数据格式 |
| 4 | **P0 致命** | `ChartBuilder.build()` 签名变更会破坏 FreeStyle Pipeline 调用 `ppt_renderer.py:499` | 采用向后兼容签名：`build(slide, chart_type_or_config, data=None, style=None, position=None, brand_colors=None)`，str 走旧逻辑，dict 走新逻辑 |
| 5 | **P1 严重** | `p.alignment = 2` 注释为 RIGHT，实际 `PP_ALIGN.RIGHT = 3`，`PP_ALIGN.CENTER = 2`，`PP_ALIGN.LEFT = 1` | 改为使用 `PP_ALIGN.RIGHT` / `PP_ALIGN.CENTER` 枚举 |
| 6 | **P1 严重** | `slide://` 内部跳转使用 `run.hyperlink._element.getparent()...` 遍历 XML 树获取 slide 对象，完全错误 | 改为通过 `prs.slides[slide_num - 1]` 获取 slide 对象，需将 `prs` 传入 `_render_links` 和 `_attach_link_to_bullet` |
| 7 | **P2 中等** | `image_grid` 中图片路径为相对路径（如 `"images/product1.png"`），但未在 `content_parser.py` 中解析为绝对路径 | 新增 `_resolve_image_grid()` 函数，与现有 `image`/`cards` 的路径解析模式一致 |

### 额外发现

| # | 描述 | 处理 |
|---|------|------|
| A | `_auto_notes` 方法中引用 `page.get("bullets")`，但自动模式路径（`_build_story_plan` else 分支）中 `PagePlan` 只有 `goal` 和 `emotion`，没有 `bullets` | 简化为仅基于 goal 生成备注 |
| B | `slide.has_notes_slide` 在首次访问 `slide.notes_slide` 前返回 `False`，之后返回 `True` | 已在约束中正确记录：只在有 notes 时调用 `slide.notes_slide` |
| C | `target_slide` API 在 python-pptx 1.0.2 中可用（已验证），但属实验性 | 已用 try/except 包裹 |
| D | 水印透明度 XML 操作：`sp.find('.//ns:solidFill')` 找到的是 run 级别的 solidFill（非 spPr 级别），但后续 `solidFill.find('ns:srgbClr')` 仍能正确找到 srgbClr | 代码逻辑正确，但变量名 `spPr` 有误导性，已修正为 `solidFill_el` |
| E | 水印旋转 XML：`sp.find('.//ns:xfrm')` 找到的是 spPr 下的 xfrm（shape transform），是正确的旋转目标 | 逻辑正确 |

### API 验证清单

| API | 验证结果 |
|-----|---------|
| `slide.notes_slide` | ✅ 首次访问自动创建，`has_notes_slide` 正确反映状态 |
| `notes_slide.notes_text_frame.text = "..."` | ✅ 支持多段落（`\n` 分隔） |
| `run.hyperlink.address = "https://..."` | ✅ 外部链接 |
| `run.hyperlink.target_slide = prs.slides[n]` | ✅ 内部跳转（实验性，需 try/except） |
| `slide.shapes.add_chart(XL_CHART_TYPE.XXX, ...)` | ✅ 11 种类型全部验证 |
| `chart.plots[0].series[i].format.fill.solid()` | ✅ 系列着色 |
| `chart.has_legend` / `chart.legend.position` | ✅ 图例控制 |
| `plot.has_data_labels` / `data_labels.font.size` | ✅ 数据标签 |
| `chart.has_title` / `chart_title.text_frame` | ✅ 图表标题 |
| `RGBColor.from_string("2563EB")` | ✅ 颜色转换 |
| `etree.SubElement(srgbClr, ns+'alpha')` | ✅ 透明度 XML |
| `xfrm.set('rot', str(int(degrees * 60000)))` | ✅ 旋转 XML |
| `sp.getparent().remove(sp)` / `insert(1, sp)` | ✅ slide 重排序 |
| `PP_ALIGN.LEFT=1, CENTER=2, RIGHT=3` | ✅ 对齐枚举 |
| `XyChartData` for scatter | ✅ 散点图专用数据 |
| `CategoryChartData` for non-scatter | ✅ 其他图表数据 |

### 现有测试基线

```
375 passed in 13.42s (python -m pytest tests/ -q)
```
